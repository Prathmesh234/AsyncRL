# DisGenerator Requirements
# High-throughput trajectory generation with vLLM

# Core async HTTP
aiohttp

# vLLM for serving (install separately based on CUDA version)
vllm

# Azure Service Bus for tool communication
azure-servicebus

# Environment management
python-dotenv

# Tokenization (for prompt_ids)
transformers

# Utilities
tqdm
